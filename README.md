# BASRUN.COM (Static Marketing Site)

This repository contains a **pure static** marketing + docs mock for **BASRUN** (Basil Runtime).

## Preview locally

No build step required.

1. Open `index.basil` in your browser.
2. Navigate with the top nav (Home / Docs / FAQ / Pricing).

Tip: for best results, serve the folder with any static server so relative links behave like production.

### Example (optional)

- VS Code: install â€œLive Serverâ€ and click **Go Live**.

## Notes

- Register/Login/Dashboard are UI-only mocks. Submits are prevented and show a â€œDemo onlyâ€ toast.
- Minimal vanilla JS is used for mobile navigation, demo toasts, a modal mock, and a theme toggle.
